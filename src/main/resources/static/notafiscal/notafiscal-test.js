'use strict';

describe('app.notafiscal', function() {

  beforeEach(module('app.notafiscal'));

  describe('NotaFiscalListCtrl', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('NotaFiscalListCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });

  describe('NotaFiscalEditCtrl', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('NotaFiscalEditCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
